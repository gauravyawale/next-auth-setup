import { Typography } from "antd";

export default function Home() {
  return (
    <Typography.Text>
      Customized login with Google and Credentials provider using next auth in
      next.js
    </Typography.Text>
  );
}
